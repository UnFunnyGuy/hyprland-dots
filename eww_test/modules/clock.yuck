;; Time vars
(defpoll DATE :interval "1s" "date '+%d/%m %A'")
(defpoll TIME :interval "1s" "date '+ %H:%M'")


(defwidget clock []
    (box 
        :class "clockbox"
        :orientation "h"
        :space-evenly false
        :halign "center"
        :style "border-radius: 15px; background-color: white; padding: 3px; margin: 3px;"
            (label 
                :class "clock-icon" 
                :text "󪤆"
                :style "box-shadow: 2px 2px 4px 0px rgba(0,0,0,0.75); border-radius: 10px; background-color: black; padding: 8px; margin: 2px; margin-left:10px;"
            )
            (label 
                :class "clock-text" 
                :text TIME
                :style "font-size: 18px; font-weight: bold; margin-right: 10px; margin-left:5px; color: black;"
            )
     )
)